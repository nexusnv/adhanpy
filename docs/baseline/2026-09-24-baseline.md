# Baseline: untouched test suite + coverage (pre-fix benchmark)

- **Date:** 2026-09-24 UTC
- **Benchmark commit:** `f9cfed5` (`upgrade black`) — `dev` == `origin/master` == `upstream`-mirror, untouched.
- **Branch:** `ci-baseline-code-coverage` (no `src/` changes; this file only).
- **Purpose:** record the green baseline before any bugfix work (see issues #2, #3). Future fix PRs target `dev`, never `master`.

## Test results (suite unmodified, modern runner)

Runner: `pytest 9.1.1` + `pytest-cov 7.1.0` + `pytest-mock 3.15.1`, `python -m pytest -q -p no:cacheprovider`.
(`pyproject.toml` `addopts = --cov=adhanpy`; no fail-under configured.)

| Interpreter | Result |
|---|---|
| CPython 3.12.13 | 98 passed |
| CPython 3.13.14 | 98 passed |
| CPython 3.14.6 | 98 passed |
| CPython 3.15.0b2 | 98 passed (pre-release, informational) |

## Coverage summary (`--cov=adhanpy --cov-branch`, Python 3.14.6)

- Line coverage: **100%** (482 stmts, 0 missed). Every other file not listed below is 100%.
- Branch coverage: **99%** — 2 partial branches, both pointing at known issues (#2):

| File | Cover | Partial branch |
|---|---|---|
| `src/adhanpy/PrayerTimes.py` | 99% | `185->192`: falsy `temp_asr` path never taken (`datetime` always truthy — dead branch) |
| `src/adhanpy/calculation/Madhab.py` | 93% | `16->exit`: implicit-`None` fallthrough for unknown madhab (bug #2.6) |

## Untouched CI steps (as defined in `.github/workflows/test.yml`)

Workflow matrix: `["3.9", "3.10"]` only; `checkout@v3` / `setup-python@v4` (deprecated).
Local replay on Python 3.14.6 with pinned dev tools:

| Step | Result |
|---|---|
| `black==24.3.0 --check src/` | pass — 26 files unchanged |
| `mypy==1.9.0 src` | pass — no issues in 26 files |
| `pytest` (pinned, see below) | **FAIL — toolchain, not library code** |

## Known toolchain breakage (evidence)

Untouched `requirements.txt` installed on Python 3.14, then `python -m pytest`:

```text
File ".../site-packages/_pytest/assertion/rewrite.py", line 962, in visit_Name
    inlocs = ast.Compare(ast.Str(name.id), [ast.In()], [locs])
AttributeError: module 'ast' has no attribute 'Str'
```

`pytest==7.1.2` uses the `ast.Str` alias removed in Python 3.14, so the pinned
dev toolchain cannot run the suite on 3.14 at all. The library code itself is
unaffected (98 passed above with a current runner).

## Reliability caveats (tracked in #3, not re-litigated here)

- Goldens assert minute-resolution strings (`"%I:%M %p"`) — blind to seconds/microseconds.
- `tests/util/test_CalendarUtil.py:21` certifies the `10:59:31 → 10:59` hour-carry bug; a correct fix will turn it red by design.
- `season_adjusted_*_twilight`, `TimeComponents.date_components` 24h+ rollover, invalid madhab/method, and polar-day messaging have no direct tests.

## Reproduce

```bash
uv venv --python 3.14 /tmp/baseline && uv pip install --python /tmp/baseline/bin/python -e . pytest pytest-cov pytest-mock
/tmp/baseline/bin/python -m pytest -q --cov=adhanpy --cov-branch --cov-report=term-missing
```
